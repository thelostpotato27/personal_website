---
import { getCollection } from 'astro:content';
import Base from '../../layouts/Base.astro';
import ItemCard from '../../components/ItemCard.astro';
import TagSearch from '../../components/TagSearch.astro';

const items = (await getCollection('technical')).sort(
  (a, b) => b.data.date.valueOf() - a.data.date.valueOf()
);
---

<Base title="Technical — Jeffrey Chang">
  <h1 class="section-title">Technical</h1>
  <p class="section-intro">Software projects and other technical work.</p>
  <TagSearch />
  <div class="card-grid">
    {
      items.map((item) => (
        <ItemCard
          href={`/technical/${item.id}/`}
          title={item.data.title}
          summary={item.data.summary}
          tags={item.data.tags}
        />
      ))
    }
  </div>
</Base>
