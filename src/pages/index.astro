---
import { getCollection } from 'astro:content';
import Base from '../layouts/Base.astro';
import ItemCard from '../components/ItemCard.astro';

const byDate = (a: { data: { date: Date } }, b: { data: { date: Date } }) =>
  b.data.date.valueOf() - a.data.date.valueOf();

const technical = (await getCollection('technical')).sort(byDate);
const hobbies = (await getCollection('hobbies')).sort(byDate);
const food = (await getCollection('food')).sort(byDate);
const media = (await getCollection('media')).sort(byDate);
---

<Base title="Jeffrey Chang">
  <section class="hero">
    <h1>Hi, I'm Jeffrey.</h1>
    <p class="section-intro">
      This is where I keep my projects and hobbies. Browse the technical work,
      see what I do for fun, read my food &amp; drink reviews, or see what
      I've been watching.
    </p>
  </section>

  <section>
    <div class="section-head">
      <h2 class="section-title">Technical</h2>
      <a href="/technical/">View all &rarr;</a>
    </div>
    <div class="card-grid">
      {
        technical
          .slice(0, 3)
          .map((item) => (
            <ItemCard
              href={`/technical/${item.id}/`}
              title={item.data.title}
              summary={item.data.summary}
              tags={item.data.tags}
              image={item.data.image}
              imageAlt={item.data.imageAlt}
            />
          ))
      }
    </div>
  </section>

  <section>
    <div class="section-head">
      <h2 class="section-title">Hobbies</h2>
      <a href="/hobbies/">View all &rarr;</a>
    </div>
    <div class="card-grid">
      {
        hobbies
          .slice(0, 3)
          .map((item) => (
            <ItemCard
              href={`/hobbies/${item.id}/`}
              title={item.data.title}
              summary={item.data.summary}
              tags={item.data.tags}
              image={item.data.image}
              imageAlt={item.data.imageAlt}
            />
          ))
      }
    </div>
  </section>

  <section>
    <div class="section-head">
      <h2 class="section-title">Food &amp; Drink</h2>
      <a href="/food/">View all &rarr;</a>
    </div>
    <div class="card-grid">
      {
        food
          .slice(0, 3)
          .map((item) => (
            <ItemCard
              href={`/food/${item.id}/`}
              title={item.data.title}
              summary={item.data.summary}
              tags={item.data.tags}
              image={item.data.image}
              imageAlt={item.data.imageAlt}
              rating={item.data.rating}
            />
          ))
      }
    </div>
  </section>

  <section>
    <div class="section-head">
      <h2 class="section-title">Shows, Movies &amp; Animation</h2>
      <a href="/media/">View all &rarr;</a>
    </div>
    <div class="card-grid">
      {
        media
          .slice(0, 3)
          .map((item) => (
            <ItemCard
              href={`/media/${item.id}/`}
              title={item.data.title}
              summary={item.data.summary}
              tags={item.data.tags}
              image={item.data.image}
              imageAlt={item.data.imageAlt}
              rating={item.data.rating}
            />
          ))
      }
    </div>
  </section>
</Base>

<style>
  .hero {
    margin-bottom: 2.5rem;
  }

  .hero h1 {
    font-size: 2.25rem;
  }

  section + section {
    margin-top: 2.5rem;
  }

  .section-head {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
  }

  .section-head a {
    color: var(--accent);
    text-decoration: none;
    font-size: 0.9rem;
  }
</style>
